import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      light: "#50c89f",
      main: "#25BB87",
      dark: "#19825e",
      contrastText: "#fff",
    },
    secondary: {
      light: "#865ff2",
      main: "#6837EF",
      dark: "#4826a7",
    },
  },
});

export default theme;
